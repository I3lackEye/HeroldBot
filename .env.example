#.env.example
# Discord Bot Token
DISCORD_TOKEN=here-goes-your-bot-token

# Debug-Mode (extended output and matchmaker logging)
DEBUG=true

# Encryption key for game keys (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your-encryption-key-here